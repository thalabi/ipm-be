package com.kerneldc.ipm.rest.csv.service.transformer;

public enum TransformationStageEnum {
	// There could be a maximum of three stages. Increase if required.
	STAGE_ONE, STAGE_TWO, STAGE_THREE;
}
